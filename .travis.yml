languaje: python
python:
   - "3.5"
#command to install dependencies
install: "pip3 install --user coverage"
- pip3 install --user mock==2.0
#command to run tests
script:
   - coverage run test.py
   - coverage report
